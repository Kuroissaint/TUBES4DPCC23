pipeline {
    agent any
    
    stages {
        stage('Deploy Dispatch Service to AKS') {
            steps {
                // Memastikan Jenkins mengeksekusi manifes dispatch-service yang baru di-update Mudrik
                sh 'kubectl apply -f dispatch-service/deployment.yaml'
            }
        }
    }
}