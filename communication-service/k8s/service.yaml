apiVersion: v1
kind: Service
metadata:
  name: communication-service
spec:
  selector:
    app: communication-service
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
  type: ClusterIP