apiVersion: v1
kind: Service
metadata:
  name: communication-service
spec:
  selector:
    app: communication-service
  ports:
    - protocol: TCP
      port: 8009
      targetPort: 8009
  type: ClusterIP