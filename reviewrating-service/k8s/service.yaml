apiVersion: v1
kind: Service
metadata:
  name: review-rating-service
spec:
  selector:
    app: review-rating-service
  ports:
    - protocol: TCP
      port: 8008
      targetPort: 8008
  type: ClusterIP